const Loader = () => {
  return (
    <div className="flex items-center justify-center">
      <div className="h-[50px] w-[50px] animate-spin rounded-full border-4 border-[var(--color-gray)] border-t-[var(--color-icon)] border-solid"></div>
    </div>
  );
};

export default Loader;